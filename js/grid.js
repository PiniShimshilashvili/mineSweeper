'use strict'

var mineGrid

function buildGrid(level){
    var grid = []
    for (var i = 0; i < level; i++){
        grid.push([]);
        for (var j = 0; j < level; j++){
        }
    }
    
}
